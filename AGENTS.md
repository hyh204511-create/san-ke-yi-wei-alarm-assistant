# Project instructions

## Current truth

- 当前真实实现是 `browser-extension/` 中的只读采集扩展和本机 JSONL 服务。
- `prototypes/` 是交互演示，不是实现证据。
- 未经项目负责人和客户明确授权，不得实现或触发文本下发、语音对讲、查岗响应、正报、申诉、忽略等动作。
- “发现接口”不等于“已交付能力”；真实状态必须写成已实现、已验证、待联调或未实现。

## Data safety

- 永远不要提交 `private/`、`browser-extension/collector-data/` 或真实平台抓包。
- 不在代码、测试、文档、Issue 或 PR 中写入账号、密码、Cookie、Authorization、Token、手机号、身份证号、真实车牌、司机姓名、经纬度或证据 URL。
- 演示数据必须明确为模拟值；对外材料默认脱敏。
- 平台静态资源、原始视频和拆帧只保存在本地私有目录，不上传第三方服务。

## Development

- 先读相关文档、调用链和测试，再做最小安全改动。
- 不新增无必要依赖、后台、框架或抽象；当前 Node 服务只使用标准库。
- 修改采集规则、脱敏、队列、落盘或报警提取逻辑时，必须留下最小回归测试。
- 保持页面无侵入：不改变省平台请求参数，不额外高频轮询，不影响视频和人工操作。

## Validation

```bash
cd browser-extension
npm test
```

涉及运行链路时还应验证：

```bash
npm start
curl http://127.0.0.1:17321/health
```

浏览器真实联调必须使用用户已授权的会话，并在交付中说明账号权限、页面版本、已验证入口和未验证边界。

### 已打开浏览器连接规则

- 用户已打开的浏览器中包含真实平台页面和已授权会话；真实联调默认从该现有页面开始，不得把空白页、沙箱页或新建页面当作真实平台依据。
- 真实平台验证必须连接用户已经打开的浏览器窗口和已登录标签；先读取已打开标签，再按当前返回的标题、URL 和标签 ID 精确接管目标标签。
- 禁止为真实平台验证新开浏览器、创建新标签或使用独立空白会话绕过用户现有登录状态；不得猜测或手写标签 ID。
- 接管失败、找不到目标标签或当前会话不是用户授权会话时，必须标记“真实平台未验证”，不得用沙箱、静态页面或新开的浏览器冒充真实验证。
- 验证完成后默认释放控制权并保留用户原有标签；只有用户明确要求时才保留交付页或未完成的登录/人工确认页。

## 提交与版本同步

- GitHub 目标账号为 `https://github.com/hyh204511-create`；账号主页不能直接作为 Git remote，首次推送前必须确认具体仓库。
- 每次功能、采集规则或安全规则变更，都必须运行测试和语法检查，形成独立 Git commit，并推送到已确认的远程分支，确保可以回退到正确版本。
- 推送前检查 `git remote -v`、`git status`、`git diff --check` 和提交内容；禁止 `git push --force`，禁止把 `private/`、`collector-data/`、真实抓包、数据库副本、证据文件或凭证上传。
- 如果本运行副本不是 Git 工作树，先同步到受控 Git 工作树并记录 commit；不得把“仅修改了本地运行副本”当成已提交。
- 交付必须说明 commit、远程分支、验证命令、推送结果和未验证边界；未确认具体仓库时标记为“待推送”。

## 部署与交付

- 每次功能、采集规则或安全规则开发完成，并完成测试、语法检查、Git commit 和远程推送后，使用指定部署技能部署当前已确认的 commit：
  [`deploy-three-passenger-ecs-rds`](https://github.com/liao3841054/san-ke-yi-wei-ai-alarm-assistant/tree/main/.agents/skills/deploy-three-passenger-ecs-rds)。
- 部署前必须确认代码目录、远程仓库、目标 commit、目标 ECS/RDS 环境和部署权限；不得把未提交的本地修改或错误分支部署到服务器。
- 必须先读取并遵循该技能的完整说明；如果技能未安装或无法读取，标记“部署待执行”，不得自行假设 ECS、RDS、迁移、服务重启或回滚步骤。
- 部署完成后必须记录：部署 commit、目标环境、执行结果、数据库迁移结果、服务健康检查、关键日志检查、回滚点和未验证边界。部署失败时保留失败证据摘要并停止继续扩大影响。
- 服务器部署只使用环境变量或受控密钥管理注入凭证；禁止把云账号、SSH 私钥、RDS 密码、生产数据、数据库副本、真实抓包或证书提交到 Git 或部署包。
- 浏览器扩展真实平台联调仍必须遵守“已打开浏览器连接规则”；ECS/RDS 部署成功不等于真实平台动作已授权或已验证。

## Documentation

### 实时报警采集边界

- 实时监控页 `#/vehicle-monitor/real-time` 中，`getVideoUnprocessedAlarm` 和 `alarmKind=0` 的 SharedWorker 消息是 `PREWARNING`，不得因为同一路由直接升级为正式实时报警。
- 平台通过 SharedWorker 推送的 `funCode=ALARM_ADD` 且 `alarmKind=1` 才归类为 `REALTIME`；page hook 只能旁路监听并脱敏上报，不得拦截、改写或主动发送平台消息。
- `alarmPreProcessingInfo` 返回的 `PENDING` 是正式报警，与 `REALTIME` 在插件面板和正式报警队列中归为同一类；内部仍保留 `sourceKind`、来源接口和来源排序，不能把 `PREWARNING` 合并进来。
- `alarmQueryList` 的分类必须同时使用请求发起时的路由和响应完成时已经渲染的活动页签；活动页签在发起时为空时，响应阶段按原路由重判，避免正式实时报警被误记为历史查询；活动页签仍无法确认时只能记为 `alarm-center-discovery`，不得伪装为 `HISTORY`。
- 浏览器真实验证必须使用当前 `browser-extension/` 解压目录；仓库中的旧 `browser-extension.crx` 不代表当前源码。源码变更后由用户重新加载扩展并按既有授权流程让已打开页面重新注入，助手不得点击扩展管理页或平台业务按钮。

- `docs/product/`：需求和建设范围。
- `docs/engineering/`：平台接入与采集实现。
- `docs/decisions/`：已形成的范围和风险决策。
- `docs/research/`：视频、页面等研究材料。
- `docs/handoff/`：接手说明。
- `docs/deliverables/`：客户交付快照；内容源与版式文件可能存在版本差异。
- `docs/archive/`：仅供追溯，不作为当前事实依据。

## 当前单条超速预报警真实测试边界（2026-07-23）

- 项目负责人已审核通过 `speeding-female-8k-mono.pcm` 和固定话术；批准范围仅限值班员在插件详情中明确确认的“当前一条超速驾驶预报警”。
- 原始来源必须始终保留为 `PREWARNING`，仅以 `testPromotion=true`、`effectiveActionKind=TEST_FORMAL` 进入测试处置；不得批量升级为正式报警，不得自动处理其他预报警。
- 真实动作完全由插件执行：插件核对报警行并进入对应车辆监控，严格按“固定语音 PCM → 固定文本/终端 TTS → 平台登记已处理”顺序执行。Agent 只能只读观察和记录，不得点击报警行、语音、文本、已处理或扩展管理页按钮。
- 语音明确失败时可发送固定文本兜底，但结果必须转人工关注；语音或任一平台请求结果为 `UNKNOWN`、超时或被阻断时，立即停止后续动作且不得自动重试。
- 平台“已处理”只表示固定动作得到明确成功回执，不表示司机已经降速；司机纠正情况继续由人工监听、查看画面和确认。
- 多账号并发必须先取得后端 `RESPONSE_PLAN` 全局租约；同一报警一旦尝试、成功或结果未知，不得由另一账号再次自动执行。
- 平台 Bearer 认证只允许插件后台通过 `webRequest` 从当前标签已发出的平台 API 请求中短期观察，按标签仅保存在内存中，最多两分钟；禁止写入存储、日志、审计、页面消息、抓包文件或 Git。传给隔离执行器后仅用于固定白名单接口。
- 源码修改后必须由用户自行在扩展管理页重新加载当前 `browser-extension/` 解压目录并刷新既有平台标签。重新加载前以及插件尚未产生真实成功回执前，只能写“代码和只读契约已验证”，不得写“实车下发已验证”。

## 五来源自动报表边界（2026-07-23）

- 五个来源固定为 `ALARM_DISPOSAL_RATE`、`ALARM_PROCESSING_RATE`、`ALARM_CENTER`、`VEHICLE_BASE_INFO`、`TRACK_COMPLETENESS`；生产运行使用确定性代码，不调用运行时大模型。
- Django 由报表管理员创建和审核任务；具有 `report.collect` 权限且已认领班次的采集员才能领取任务、分页上传和结束来源。任务租约绑定实名采集员、设备和省平台账号，禁止跨账号或跨设备复用。
- 插件只在用户现有已登录省平台标签中执行审核后的白名单契约。五个真实契约在人工走完来源并确认方法、路径、筛选、分页、响应字段和导出范围前必须保持 `UNVERIFIED`，不得猜接口、领取任务、点击查询/导出或发起省平台请求。
- 契约观察只保留方法、净化路径和字段结构；Cookie、Authorization、Token、验证码、密码和真实业务值不得上传 Django、写入本地契约文件或提交 Git。
- 五来源契约发现只能旁路记录人工已经触发的 fetch/XHR。候选生成器必须保持 `enabled=false`；只有五个来源分别具有唯一方法、路径、分页、行路径、总数路径、字段签名和范围证据，并经人工审核后才能提升为正式白名单。
- 来源缺页、总页数/总行数不符、字段签名变化或查询范围无法证明时，任务保持 `DATA_INCOMPLETE`，不得生成正式报表。相同分页幂等重放返回原结果，同一幂等键内容变化返回冲突。
- 企业报警日报、周报、月报按平台企业 ID 独立拆分三个来源；车辆动态日报以车辆基础信息为左表、车辆 ID 为首选关联键，空定位进入过滤清单，未来定位、重复 ID、一对多、企业冲突和待审核车牌匹配进入关键异常并阻断发布。
- 报表模板只允许使用净化后的 `ALARM_THREE_SHEET_V1` 和 `VEHICLE_MONITOR_DAILY_V1`；真实平台数据、原始样表中的企业/车辆内容和未脱敏抓包不得进入 Git。
- Agent 对真实平台只负责连接现有浏览器进行只读观察和记录，不得点击报表查询、导出或业务动作按钮。当前只能声明代码、合成样本和安全阻断测试通过；五来源真实接口与真实报表结果仍需授权人员人工走查后验收。
- 报警处理状态使用 `UNPROCESSED / EXECUTING / PROCESSED / MANUAL_REQUIRED / UNKNOWN`，不得用单一布尔值表示。服务器确认的 `PROCESSED` 与 `UNKNOWN` 不得被其他账号的旧插件快照回退，并继续作为跨账号重复下发的阻断依据。
